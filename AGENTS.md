# RF-FIP Project Agent

Read `./rules.md` before changing files in this project.

## Scope

This directory contains the active RF Failure Intelligence Platform app.

## Required Context

- Start with `docs/README.md`.
- For implementation, read the relevant docs contract first.
- For phase work, read `phases/index.json` and the active `stepN.md`.

## Defaults

- Keep changes small and reversible.
- Prefer existing React, Express, Vite, and local helper patterns.
- Verify with `docs/TEST_PLAN.md` before claiming completion.

## Escalation

- Update nearest `rules.md` when a step fails, blocks, or reveals a reusable caution.
- Use ADR only for durable architecture decisions.
