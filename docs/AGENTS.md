# Docs Agent

Read `./rules.md` before editing documentation.

## Scope

This directory stores product, architecture, API, UI, regression, and test contracts.

## Defaults

- Keep docs concise and stable.
- Prefer contracts and decisions over logs.
- Link to source files instead of copying long code.

## Phase Rule

Every generated phase must reference `docs/README.md` and the relevant spec files.
