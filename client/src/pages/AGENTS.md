# Pages Agent

Read `./rules.md` before editing page-level workflows.

## Scope

Pages coordinate workflow state and route-level behavior.

## Required Docs

- `../../../docs/UI_UX_SPEC.md`
- `../../../docs/IMPORT_SPEC.md`

## Defaults

- Keep `Home.tsx` workflow changes scoped and verified.
- Extract reusable UI only when duplication becomes meaningful.
