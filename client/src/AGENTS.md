# Client Src Agent

Read `./rules.md` before editing `client/src`.

## Scope

Application UI, components, hooks, contexts, and frontend helpers.

## Defaults

- Page workflows belong in `pages/`.
- Reusable display logic belongs in `components/`.
- Client API wrappers belong in `lib/`.
- Keep UI state local unless a durable API contract exists.
