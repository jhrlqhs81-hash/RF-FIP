# Client Rules

- Do not call persistence storage directly from UI code.
- Do not put provider secrets in client code.
- Icon-only buttons require an accessible label.
- Recoverable form/API failures must preserve user input.
- Import original source views must not truncate raw text.
